<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spldeolin.beginningmind.app.mapper.ProductsMapper">
    <!-- [START] Any modifications may be overwritten by future code generations. -->

    <resultMap id="all" type="com.spldeolin.beginningmind.app.entity.ProductsEntity">
        <id column="productCode" property="productCode"/>
        <result column="productName" property="productName"/>
        <result column="productLine" property="productLine"/>
        <result column="productScale" property="productScale"/>
        <result column="productVendor" property="productVendor"/>
        <result column="productDescription" property="productDescription"/>
        <result column="buyPrice" property="buyPrice"/>
        <result column="MSRP" property="msrp"/>
    </resultMap>

    <sql id="all">
        `productCode`, `productName`, `productLine`, `productScale`, `productVendor`, `productDescription`, `buyPrice`,
        `MSRP`
    </sql>

    <insert id="insert" parameterType="com.spldeolin.beginningmind.app.entity.ProductsEntity">
        <!-- @formatter:off -->
        INSERT INTO `products`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="productCode!=null"> `productCode`, </if>
            <if test="productName!=null"> `productName`, </if>
            <if test="productLine!=null"> `productLine`, </if>
            <if test="productScale!=null"> `productScale`, </if>
            <if test="productVendor!=null"> `productVendor`, </if>
            <if test="productDescription!=null"> `productDescription`, </if>
            <if test="buyPrice!=null"> `buyPrice`, </if>
            <if test="msrp!=null"> `MSRP`, </if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="productCode!=null"> #{productCode}, </if>
            <if test="productName!=null"> #{productName}, </if>
            <if test="productLine!=null"> #{productLine}, </if>
            <if test="productScale!=null"> #{productScale}, </if>
            <if test="productVendor!=null"> #{productVendor}, </if>
            <if test="productDescription!=null"> #{productDescription}, </if>
            <if test="buyPrice!=null"> #{buyPrice}, </if>
            <if test="msrp!=null"> #{msrp}, </if>
        </trim>
        <!-- @formatter:on -->
    </insert>

    <insert id="batchInsert">
        <!-- @formatter:off -->
        <foreach collection="entities" item="one" separator=";">
            INSERT INTO `products`
            <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="one.productCode!=null"> `productCode`, </if>
                <if test="one.productName!=null"> `productName`, </if>
                <if test="one.productLine!=null"> `productLine`, </if>
                <if test="one.productScale!=null"> `productScale`, </if>
                <if test="one.productVendor!=null"> `productVendor`, </if>
                <if test="one.productDescription!=null"> `productDescription`, </if>
                <if test="one.buyPrice!=null"> `buyPrice`, </if>
                <if test="one.msrp!=null"> `MSRP`, </if>
            </trim>
            <trim prefix="VALUE (" suffix=")" suffixOverrides=",">
                <if test="one.productCode!=null"> #{one.productCode}, </if>
                <if test="one.productName!=null"> #{one.productName}, </if>
                <if test="one.productLine!=null"> #{one.productLine}, </if>
                <if test="one.productScale!=null"> #{one.productScale}, </if>
                <if test="one.productVendor!=null"> #{one.productVendor}, </if>
                <if test="one.productDescription!=null"> #{one.productDescription}, </if>
                <if test="one.buyPrice!=null"> #{one.buyPrice}, </if>
                <if test="one.msrp!=null"> #{one.msrp}, </if>
            </trim>
        </foreach>;
        <!-- @formatter:on -->
    </insert>

    <insert id="batchInsertEvenNull">
        <!-- @formatter:off -->
        INSERT INTO `products`
            ( <include refid="all"/> )
        VALUES
            <foreach collection="entities" item="one" separator=",">(
                #{one.productCode}, #{one.productName}, #{one.productLine}, #{one.productScale}, #{one.productVendor},
                #{one.productDescription}, #{one.buyPrice}, #{one.msrp}
            )</foreach>
        <!-- @formatter:on -->
    </insert>

    <update id="batchUpdate">
        <foreach collection="entities" item="one" separator=";">
            <!-- @formatter:off -->
            UPDATE `products`
            <set>
                <if test="one.productName!=null"> `productName` = #{one.productName}, </if>
                <if test="one.productLine!=null"> `productLine` = #{one.productLine}, </if>
                <if test="one.productScale!=null"> `productScale` = #{one.productScale}, </if>
                <if test="one.productVendor!=null"> `productVendor` = #{one.productVendor}, </if>
                <if test="one.productDescription!=null"> `productDescription` = #{one.productDescription}, </if>
                <if test="one.buyPrice!=null"> `buyPrice` = #{one.buyPrice}, </if>
                <if test="one.msrp!=null"> `MSRP` = #{one.msrp}, </if>
            </set>
            WHERE TRUE
              AND `productCode` = #{one.productCode}
            <!-- @formatter:on -->
        </foreach>
    </update>

    <update id="batchUpdateEvenNull">
        <foreach collection="entities" item="one" separator=";">
            <!-- @formatter:off -->
            UPDATE `products`
            SET
                `productName` = #{one.productName},
                `productLine` = #{one.productLine},
                `productScale` = #{one.productScale},
                `productVendor` = #{one.productVendor},
                `productDescription` = #{one.productDescription},
                `buyPrice` = #{one.buyPrice},
                `MSRP` = #{one.msrp}
            WHERE TRUE
              AND `productCode` = #{one.productCode}
            <!-- @formatter:on -->
        </foreach>
    </update>

    <select id="queryById" parameterType="String" resultMap="all">
        <!-- @formatter:off -->
        SELECT
            <include refid="all"/>
        FROM `products`
        WHERE TRUE
          AND `productCode` = #{productCode}
        <!-- @formatter:on -->
    </select>

    <update id="updateById" parameterType="com.spldeolin.beginningmind.app.entity.ProductsEntity">
        <!-- @formatter:off -->
        UPDATE `products`
        <set>
            <if test="productName!=null"> `productName` = #{productName}, </if>
            <if test="productLine!=null"> `productLine` = #{productLine}, </if>
            <if test="productScale!=null"> `productScale` = #{productScale}, </if>
            <if test="productVendor!=null"> `productVendor` = #{productVendor}, </if>
            <if test="productDescription!=null"> `productDescription` = #{productDescription}, </if>
            <if test="buyPrice!=null"> `buyPrice` = #{buyPrice}, </if>
            <if test="msrp!=null"> `MSRP` = #{msrp}, </if>
        </set>
        WHERE TRUE
          AND `productCode` = #{productCode}
        <!-- @formatter:on -->
    </update>

    <update id="updateByIdEvenNull" parameterType="com.spldeolin.beginningmind.app.entity.ProductsEntity">
        <!-- @formatter:off -->
        UPDATE `products`
        SET
            `productName` = #{productName},
            `productLine` = #{productLine},
            `productScale` = #{productScale},
            `productVendor` = #{productVendor},
            `productDescription` = #{productDescription},
            `buyPrice` = #{buyPrice},
            `MSRP` = #{msrp}
        WHERE TRUE
          AND `productCode` = #{productCode}
        <!-- @formatter:on -->
    </update>

    <select id="queryByIds" parameterType="String" resultMap="all">
        <!-- @formatter:off -->
        SELECT
            <include refid="all"/>
        FROM `products`
        WHERE TRUE
          AND `productCode` IN (<foreach collection="productCodes" item="one" separator=",">#{one}</foreach>)
        <!-- @formatter:on -->
    </select>

    <select id="queryByIdsEachId" parameterType="String" resultMap="all">
        <!-- @formatter:off -->
        SELECT
            <include refid="all"/>
        FROM `products`
        WHERE TRUE
          AND `productCode` IN (<foreach collection="productCodes" item="one" separator=",">#{one}</foreach>)
        <!-- @formatter:on -->
    </select>

    <select id="queryByEntity" parameterType="com.spldeolin.beginningmind.app.entity.ProductsEntity" resultMap="all">
        <!-- @formatter:off -->
        SELECT
            <include refid="all"/>
        FROM `products`
        WHERE TRUE
          <if test="productCode!=null"> AND `productCode` = #{productCode} </if>
          <if test="productName!=null"> AND `productName` = #{productName} </if>
          <if test="productLine!=null"> AND `productLine` = #{productLine} </if>
          <if test="productScale!=null"> AND `productScale` = #{productScale} </if>
          <if test="productVendor!=null"> AND `productVendor` = #{productVendor} </if>
          <if test="productDescription!=null"> AND `productDescription` = #{productDescription} </if>
          <if test="buyPrice!=null"> AND `buyPrice` = #{buyPrice} </if>
          <if test="msrp!=null"> AND `MSRP` = #{msrp} </if>
        <!-- @formatter:on -->
    </select>

    <select id="listAll" resultMap="all">
        <!-- @formatter:off -->
        SELECT
            <include refid="all"/>
        FROM `products`
        WHERE TRUE
        <!-- @formatter:on -->
    </select>

    <insert id="insertOrUpdate" parameterType="com.spldeolin.beginningmind.app.entity.ProductsEntity">
        <!-- @formatter:off -->
        INSERT INTO `products`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="productCode!=null"> `productCode`, </if>
            <if test="productName!=null"> `productName`, </if>
            <if test="productLine!=null"> `productLine`, </if>
            <if test="productScale!=null"> `productScale`, </if>
            <if test="productVendor!=null"> `productVendor`, </if>
            <if test="productDescription!=null"> `productDescription`, </if>
            <if test="buyPrice!=null"> `buyPrice`, </if>
            <if test="msrp!=null"> `MSRP`, </if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="productCode!=null"> #{productCode}, </if>
            <if test="productName!=null"> #{productName}, </if>
            <if test="productLine!=null"> #{productLine}, </if>
            <if test="productScale!=null"> #{productScale}, </if>
            <if test="productVendor!=null"> #{productVendor}, </if>
            <if test="productDescription!=null"> #{productDescription}, </if>
            <if test="buyPrice!=null"> #{buyPrice}, </if>
            <if test="msrp!=null"> #{msrp}, </if>
        </trim>
        ON DUPLICATE KEY UPDATE
        <trim suffixOverrides=",">
            <if test="productName!=null"> `productName` = #{productName}, </if>
            <if test="productLine!=null"> `productLine` = #{productLine}, </if>
            <if test="productScale!=null"> `productScale` = #{productScale}, </if>
            <if test="productVendor!=null"> `productVendor` = #{productVendor}, </if>
            <if test="productDescription!=null"> `productDescription` = #{productDescription}, </if>
            <if test="buyPrice!=null"> `buyPrice` = #{buyPrice}, </if>
            <if test="msrp!=null"> `MSRP` = #{msrp}, </if>
        </trim>
        <!-- @formatter:on -->
    </insert>

    <!-- [END] Any modifications may be overwritten by future code generations. -->
</mapper>